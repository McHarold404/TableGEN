Instruction:
According to the live text, please generate a summary table summarizing he different number of events of both teams.
Note that goals and saved attempts and blocked attempts and missed attempts are considered shots. Handball and dangerous play are also considered foul. The second yellow card is also considered a red card. Penalty is also considered as free kicks.

Task:
Perform the following task:
1. Generate python code to integrate the given tuples and execute it. 
2. Output a table with 2 rows in CSV format.

**Output Table Instructions**:
1. The first row of the table should contain the headers.
2. The first column header is Team and contains Home Team and Away Team as rows.
3. The values of all the cells should be strictly positive numbers.
4. Use "|" as a separator between cells.
5. Every row should be in a separate line
6. Return the final table with the heading ### Final Table

**Output Format**:

### Final Table:
| Team | <Column1> | ... | 
| Home Team | <Cell Value for (Home Team, Column Header 1)> | Value2 | ... | 
| Away Team |  <Cell Value for (Away Team, Column Header 1)> | ... |


Table name leads with a ###, followed by the table where values are separated by the symbol '|' and rows are separated by '\n'.
Use '|' as the only separator/delimiter.
Empty cell values are filled as "None".

Provide the output in the specified format only.