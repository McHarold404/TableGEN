You are an expert at defining structural tables by identifying the relevant column headers and row headers through given text statements.

TASK:
I want you to extract row and column headers from the text.
It is known that some attributes can be directly mentioned in the text and some might have to be inferred. 
We want to capture all the information that is present in the text and want to extract all the row and column headers that would be needed to minimise information loss(zero loss).
The statements are in sequential order of time i.e. the nth statement would occur after the (n-1)th statement.
Group relevant informations.

Provide outputs in a json format with row and column headers.
Return only the output state without any reasoning steps, commentory, or any extra words at the start or end of the output.

The statements are:
