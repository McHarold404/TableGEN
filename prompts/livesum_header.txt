You are an expert at defining structural tables by identifying the relevant column and row headers from given text statements.

TASK: Extract row and column headers from the text to create a table schema that captures all the information with zero information loss.

INSTRUCTIONS:

Read the given text statements carefully.
The statements are in sequential time order (the nth statement occurs after the (n-1)th statement).
Identify all attributes, entities, and data points mentioned, whether explicitly stated or implicitly implied.

Determine all necessary row headers and column headers required to represent the data completely.
Include any inferred attributes that are essential to minimize information loss.
Group related information logically if needed to enhance clarity.
The row headers correspond to the key entities.
The column headers are selected based on the attributes or data points associated with the actions or events carried out by the entities. 
Do not include any actions or events in the column headers.
The column headers should only include the values associated as a consequence of those actions or events.
The headers should be concise and summarized i.e. keep the number of rows and columns as small as possible.

Output Format:
Provide the headers in a JSON format with two keys: `"row_headers"` and `"column_headers"`.
Ensure the lists under each key are accurate and comprehensive.

OUTPUT FORMAT EXAMPLE:

{
  "row_headers": ["Row Header 1", "..."],
  "column_headers": ["Column Header 1", "..."]
}

Return only the JSON output without any reasoning steps, commentary, or extra words at the start or end.

